FROM node:18.17.0-slim
# RUN apt-get update && apt-get install -y git
# 生成 ssh key
# RUN  git config --global user.email "server@repo.com" && git config --global user.name "server" # && ssh-keygen -t rsa -C "gitea@repo.com" -f /root/.ssh/id_rsa -q -N "" && ssh-keyscan codeup.aliyun.com >> ~/.ssh/known_hosts
